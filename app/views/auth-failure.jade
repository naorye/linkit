doctype 5
html(lang="en")
    head
        script(type='text/javascript')
            if (window.opener && window.opener.app) {
                window.opener.app.vent.trigger('authCallback', {
                    'success': false,
                    'error': '#{error}'
                });
            }
            window.close();
    body